# Laisser vide en production VM (même origine /api).
# En local, Vite proxy /api vers localhost:4000 (voir vite.config.js).
# Pour un frontend déployé sur Vercel séparément du backend, renseigner
# l'URL publique du backend, sans slash final, ex: https://ats-api.vercel.app
VITE_API_URL=
